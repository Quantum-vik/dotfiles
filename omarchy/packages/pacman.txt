# Packages installed on top of Omarchy 4 with omarchy-pkg-add (Arch's repositories plus Omarchy's).
# One per line; '#' comments ignored.
# Omarchy already ships git, curl, jq, unzip, tmux, fzf, ripgrep, fd, bat, btop, lazygit, Docker, base-devel,
# and (through mise) gh and claude, so they aren't listed.

# Shell
zsh
zsh-autosuggestions
zsh-syntax-highlighting

# Terminals (install.sh makes kitty the default)
kitty
alacritty

# Development. linux/install.sh downloads Go, kubectl and the AWS CLI; Arch packages them.
go
uv
kubectl
aws-cli-v2
python-pipx
wget
# VS Code, from Omarchy's repository
visual-studio-code-bin
# Claude's official Linux desktop app (chat, Cowork and Claude Code in a window), from Omarchy's repository.
# The `claude` CLI itself comes from mise, not from here.
claude-desktop

# Build tools for poweralertd
meson
ninja

# Apps
vlc
vlc-plugins-all
qbittorrent
zim
flatpak
# GNOME Camera
snapshot
inter-font

# Voice dictation: whisper.cpp running locally, bound to F9 (see README.md "Dictation"). wtype is how it types
# the transcript into the focused window. Both come from Omarchy's own repository.
voxtype-bin
wtype

# Sensors and per-process network/disk use
lm_sensors
nethogs
iotop
